provider "aws" {
  region = "ap-south-1"

  # can also add access and secret key here directly, instead of configuring aws_cli on machine.
  # but not recommended, can be exposed easily here

  # access_key = ""
  # secret_key = ""

}
